export function DemoBadge() {
  return (
    <span
      className="badge border border-[var(--border)] bg-[var(--bg-elevated)] text-[var(--muted)]"
      title="Preview profile for platform demonstration — not a real individual"
    >
      Preview creator
    </span>
  );
}
